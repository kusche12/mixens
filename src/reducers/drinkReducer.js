const initialState = {
    something: false
};


const drinkReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default drinkReducer;